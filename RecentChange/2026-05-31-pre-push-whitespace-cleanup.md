# Pre-push whitespace cleanup

## Goal

Fix trailing whitespace reported by `git diff --cached --check` before committing and pushing to GitHub.

## Scope

- Documentation whitespace.
- New route/component/test file whitespace.
- No behavior changes.
